{
  "dependencies": {
    "@observablehq/notebook-kit": "^1.5.0",
    "@turf/clusters": "^7.3.0",
    "@turf/clusters-dbscan": "^7.3.0",
    "d3-array": "^3.2.4",
    "geotoolbox": "^3.0.4",
    "geoviz": "^0.9.4"
  },
  "scripts": {
    "preview": "notebooks preview --root src",
    "build": "notebooks build --root src -- src/*.html && rm -rf docs && mkdir docs && cp -r src/.observable/dist/* docs/ && rm -rf src/.observable"
  }
}
